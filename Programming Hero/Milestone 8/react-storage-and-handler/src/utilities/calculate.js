
const add = (first, second) => {
    return first + second;
}

const multiply = (first, second) => first * second;


export { add, multiply }


// export default add; // for one function how to import